fn pow(base, power) {
    for let i = 0 i < 50 i++ base *= power
    return base
}
fn doNothing() {
    // Do nothing
}
fn loop(n) {
    while n > 0 {
        n--;
        doNothing();
    }
}
fn main() {
    let result = pow((1 + 2) & (3 + 4) - ((3 ^ ~6) & 31) * (7 & 8 | 9), 2)
    loop(result)
    if result > 0 doNothing()
}
