using stdlib;

fn causeStackOverflow<T>(i: int, t: T): [T] {
    if(i > 0) {
        return causeStackOverflow<[T]>(i - 1, [t])[0];
    } else {
        return [t];
    }
}
entrypoint fn main() {
    causeStackOverflow<bool>(3, true);
}